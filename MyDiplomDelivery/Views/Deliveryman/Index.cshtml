@model IEnumerable<MyDiplomDelivery.ViewModels.DeliveryMan.AllOrderViewModel>

<div>
    <h3>All Orders</h3>

    @if (Model.Any())
    {
        <table>
            <tr>
                <th>Name</th>
                <th>Description</th>
                <th>Status</th>
                <th>From</th>
                <th>To</th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Name</td>
                    <td>@item.Description</td>
                    <td class="status">@item.Status</td>
                    <td>@item.From</td>
                    <td>@item.To</td>
                    <td>
                        <button class="show"><a asp-controller="Deliveryman" asp-action="Order" asp-route-num="@item.Number">Редактировать</a></button>
                    </td>
                </tr>
            }

        </table>
    }
    else
    {
        <p>История пуста</p>
    }

</div>

@section Scripts{
    <script src="~/js/Color.js" asp-append-version="true"></script>
}
@section Links{
    <link rel="stylesheet" href="~/css/Orders.css" asp-append-version="true" />
}